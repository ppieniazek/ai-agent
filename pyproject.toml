[project]
name = "ai-agent"
version = "0.1.0"
description = "Boot.dev guided project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-genai==1.12.1",
    "pip>=25.1.1",
    "python-dotenv==1.1.0",
]
